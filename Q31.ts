var user_names = ["Admin", "Isha", "Muskaan", "Sabaa", "Maryam"];
if(user_names.length == 5) {
    console.log("The list is not empty");
}

var list_names = []
if(list_names.length == 5) {
    console.log("We need to find some users!");
}
